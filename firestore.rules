rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 인증된 사용자만 읽기/쓰기 허용
      allow read, write: if request.auth != null;
    }
  }
}
